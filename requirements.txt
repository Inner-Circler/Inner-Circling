anthropic==0.109.2
python-dotenv==1.2.2
tomli==2.2.1; python_version < "3.11"
tomli-w==1.2.0

# pyflakes IS NOT HERE, and its absence is the decision. It is this
# project's static gate, run by coordinator/check_lint.py from the
# pre-commit hook -- and NEITHER OF THOSE SHIPS. check_lint.py is a
# development tool, and the hook battery is not installed in a tree
# without coordinator/tests/ (see coordinator/gitrepo.py). Requiring it
# here would have you install a package nothing in this copy runs, under
# a comment naming a file you do not have. Removed 2026-08-24.
#
# If you fork this and want the gate, it is `pip install pyflakes` and
# the module is in the project's own history.
