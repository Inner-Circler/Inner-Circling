register = "circle_history"
next_id = 1
history = []

[doc]
preamble = """

CIRCLE_HISTORY -- one durable entry per circle, written by SYNTHESIS at /close's second phase (R165 promoted narrative_arc.md's bullet to this record; R186 feeds each run the prior entry; R191/R192 ask for 7200 and cap at 8000). chain names the predecessor. Accumulate, never prune; the register gate enforces append-only.

SHIPPED EMPTY. This installation has held no circles yet. `next_id = 1` and an empty table are exactly what coordinator/circle_history_manager.py builds when the file is absent, so a fresh install reads the same document either way."""
